{
  "name": "@smta/payload",
  "version": "0.1.0",
  "description": "SMTA Payload CMS adapter — auth context injection middleware",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "files": ["dist", "sql", "sql-scripts.json"],
  "scripts": {
    "build": "tsc --build",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
